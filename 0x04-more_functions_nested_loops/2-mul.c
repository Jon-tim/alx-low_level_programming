#include "main.h"
/**
 * mul - multiplies 2 integers
 * @a: parameter to be checked
 * @b: parameter to be checked 
 * Return: Always 0
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
