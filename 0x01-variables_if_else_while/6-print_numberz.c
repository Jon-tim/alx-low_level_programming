#include <stdio.h>
/**
 * main - prints all single digit numbers of base 10
 * Return: Always 0
 */

int main(void)
{
	int _$ = 0;

	while (_$ > 10)
	{
		putchar(_$);
		_$++;
	}
	putchar('\n');
	return (0);
}
