#include "main.h"
/**
 * add - return the sum of two numbers
 * @a: number to be checked
 * @b: number to be checked
 * Return: always 0
 */

int add(int a, int b)
{
	int result = a + b;

	return (result);
}
