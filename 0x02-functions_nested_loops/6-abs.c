#include "main.h"
/**
 * _abs - starting point
 * @n: number to be checked
 * Return: return whatever's stipulated
 */

int _abs(int n)
{
	if (n < 0)
	{
		n = -1 * n;
		return (n);
	}
	else
	{
		return (n);
	}
}
